package com.pfe.facturation.enums;

/**
 * Type d'entité partenaire (pour les documents).
 * Permet de distinguer si un vendeur est un client final ou une entreprise (émetteur).
 */
public enum EntityType {
    CLIENT,
    EMETTEUR
}
